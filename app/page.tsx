export default function Home() {
  return (
    <main style={{fontFamily:"Montserrat, Arial, sans-serif", padding: 48}}>
      <h1>KAM Deck Generator API</h1>
      <p>API activa. Usa los endpoints <code>/api/generate-content</code> y <code>/api/generate-deck</code> desde WordPress/Divi.</p>
    </main>
  );
}